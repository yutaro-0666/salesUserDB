declare namespace eventHandle {
  namespace create {
    type show = 'app.record.create.show' | 'mobile.app.record.create.show';
  }

  namespace edit {
    type show = 'app.record.edit.show' | 'mobile.app.record.edit.show';
  }

  namespace detail {
    type show = 'app.record.detail.show' | 'mobile.app.record.detail.show';
  }
}
